# Правило для проекта boto-chronicler
# 1. Запрещает запускать бота локально (вне Docker)
# 2. Разрешает только docker-команды для сборки/перезапуска

# Если запускается python или любой .py файл внутри ./bot, 
# проверяй: если нет переменной среды INSIDE_DOCKER — блокируй
deny run: python bot/*.py if not env:INSIDE_DOCKER

# При изменениях в папке ./bot, автоматически выполняй:
# docker-compose build && docker-compose up -d
on change in ./bot/*:
  run: docker-compose up --build -d bot

