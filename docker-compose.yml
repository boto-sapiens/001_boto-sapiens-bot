services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: boto-sapiens
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: always
    command: ["python", "bot/main.py"]
